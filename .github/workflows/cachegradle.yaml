name: Test gradle triggers

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged != true

    steps:
      - name: Test
        run: |
          echo "test"
          echo ${{github.event}}